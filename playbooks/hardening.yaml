---
- hosts: ubuntu
  become: yes

  tasks:

  - name: Sys Actualizado
    ansible.builtin.apt:
      name: "*"
      state: latest
      update_cache: true
    notify: Reboot Server

  handlers:

  - name: Reboot Server
    ansible.builtin.reboot:



